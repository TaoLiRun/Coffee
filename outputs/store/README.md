# Store Closure Outputs

This folder contains store-level closure-detection outputs. The key file for the current project is `non_uni_store_closures.csv`, which feeds the customer-store registry builder.

The current estimation scripts keep this path stable because downstream code reads it directly.
