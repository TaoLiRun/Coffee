# Customer-Store Closure Registries

- `closure_pair_registry.csv` is the canonical 18-closure main analysis registry.
- `closure_pair_registry_full.csv` preserves the previous 22-closure registry for archive and full-registry robustness comparisons.

The main registry excludes the four early-February closures with `dept_id` 254, 228, 225, and 181.
